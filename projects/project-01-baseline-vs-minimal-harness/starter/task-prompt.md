# Task prompt (Baseline run — Run One)

Build a minimal knowledge-base desktop app with:
- a document list panel on the left,
- a Q&A interaction panel on the right,
- a local data directory created on first launch,
- a window that launches and runs.

That's the whole brief. No rules, no checklist, no init script — this is the
prompt-only condition. Hand exactly this to the agent, start a timer, and record
how far it gets in `../NOTES.md`.
